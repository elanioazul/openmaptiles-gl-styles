worker_processes 1;
events { worker_connections 1024; }

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    server {
        listen 80;
        server_name localhost;

        # Optional root endpoint so visiting http://localhost:3002 doesn't 404
        location / {
            add_header Content-Type text/plain;
            return 200 'Protomaps Precompiled Sprites Nginx Server Ready';
        }

        # Serve pre-compiled Protomaps sprites
        location /pre-compiled-sprite/ {
            # MUST match the volume mount path in docker-compose.yaml
            # MUST end with a trailing slash to pair with 'location /sprite/'
            alias /data/sprites/;

            # CORS Headers for Maputnik
            add_header 'Access-Control-Allow-Origin' '*' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range' always;
            
            if ($request_method = 'OPTIONS') {
                add_header 'Access-Control-Allow-Origin' '*';
                add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
                add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range';
                add_header 'Access-Control-Max-Age' 1728000;
                add_header 'Content-Type' 'text/plain; charset=utf-8';
                add_header 'Content-Length' 0;
                return 204;
            }
        }
    }
}